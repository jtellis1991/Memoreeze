<div id="<%= dom_id(card)%>_target_form">
  <%= form_with(url: "/decks/#{deck.id}/cards/#{card.id}/target", local: false) do |form| %>
    <% if card.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(card.errors.count, "error") %> prohibited this card from being saved:</h2>

        <ul>
          <% card.errors.each do |error| %>
            <li><%= error.full_message %></li>
          <% end %>
        </ul>
      </div>
    <% end %>

        <%= form.label :target, class: "sr-only" %>
        <%= form.text_area :target, class: "form-control", value: card.target.target %>

  <%= form.submit "Update Target", class: "btn btn-outline-secondary btn-block" %>

  <% end %>
</div>