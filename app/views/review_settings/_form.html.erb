<%= form_with(model: review_setting) do |form| %>
  <% if review_setting.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(review_setting.errors.count, "error") %> prohibited this review_setting from being saved:</h2>

      <ul>
        <% review_setting.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :reviews_per_day %>
    <%= form.text_field :reviews_per_day %>
  </div>

  <div class="field">
    <%= form.label :new_per_day %>
    <%= form.number_field :new_per_day %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
