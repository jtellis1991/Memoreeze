<%= form_with(model: result) do |form| %>
  <% if result.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(result.errors.count, "error") %> prohibited this result from being saved:</h2>

      <ul>
        <% result.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :review_id %>
    <%= form.number_field :review_id %>
  </div>

  <div class="field">
    <%= form.label :again %>
    <%= form.text_field :again %>
  </div>

  <div class="field">
    <%= form.label :hard %>
    <%= form.text_field :hard %>
  </div>

  <div class="field">
    <%= form.label :good %>
    <%= form.text_field :good %>
  </div>

  <div class="field">
    <%= form.label :easy %>
    <%= form.text_field :easy %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
