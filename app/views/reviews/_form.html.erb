<h1 class="text-center mb-5">
  <%= card_account.card.illustrative_test %>
</h1>

<h3 class="text-center mb-5 collapse multi-collapse" id="solution">
  <%= card_account.card.solution %>
</h3>

<ul class="list-group list-group-flush collapse multi-collapse" id="explanation">
    <li class="list-group-item"></li>
    <li class="list-group-item"><%= card_account.card.target.explanation %></li>
</ul>

<div class="row collapse multi-collapse" id="result">
  <%# Review Again in 1 day if you got the answer %>
  <%= form_with(model: review, class: "col-6") do |form| %>
    <% if review.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(review.errors.count, "error") %> prohibited this review from being saved:</h2>

        <ul>
          <% review.errors.each do |error| %>
            <li><%= error.full_message %></li>
          <% end %>
        </ul>
      </div>
    <% end %>

    <div class="form-group font-weight-bold">
      <%= form.label :milliseconds_elapsed, class: 'sr-only' %>
      <%= form.hidden_field :milliseconds_elapsed %>
    </div>

    <div class="form-group font-weight-bold">
      <%= form.label :card_account_id, class: 'sr-only' %>
      <%= form.hidden_field :card_account_id, value: card_account.id %>
    </div>

    <div class="form-group font-weight-bold">
      <%= form.label :user_id, class: 'sr-only' %>
      <%= form.hidden_field :user_id, value: @user.id %>
    </div>

    <div class="actions form-group">
      <%= form.submit class: "btn btn-danger btn-block mt-3", name: "result", value: "Again" %>
    </div>

  <% end %>

  <%# Review Again in 1 day if you got the answer %>
  <%= form_with(model: review, class: "col-6") do |form| %>
    <% if review.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(review.errors.count, "error") %> prohibited this review from being saved:</h2>

        <ul>
          <% review.errors.each do |error| %>
            <li><%= error.full_message %></li>
          <% end %>
        </ul>
      </div>
    <% end %>

    <div class="form-group font-weight-bold">
      <%= form.label :milliseconds_elapsed, class: 'sr-only' %>
      <%= form.hidden_field :milliseconds_elapsed %>
    </div>

    <div class="form-group font-weight-bold">
      <%= form.label :card__account_id, class: 'sr-only' %>
      <%= form.hidden_field :card_id, value: card.id %>
    </div>

    <div class="form-group font-weight-bold">
      <%= form.label :user_id, class: 'sr-only' %>
      <%= form.hidden_field :user_id, value: @user.id %>
    </div>

    <div class="actions form-group">
      <%= form.submit class: "btn btn-success btn-block mt-3", name: "result", value: "Good" %>
    </div>

  <% end %>
</div>  
